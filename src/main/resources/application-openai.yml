spring:
  ai:
    ollama:
      chat:
        enabled: false
    openai:
      api-key: ${OPENAI_API_KEY}
      chat:
        options:
          model: ${OPENAI_MODEL:gpt-4o}
          temperature: 0.7

app:
  llm:
    model: ${OPENAI_MODEL:gpt-4o}
